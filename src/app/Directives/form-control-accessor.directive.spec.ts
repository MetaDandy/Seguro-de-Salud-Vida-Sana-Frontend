import { FormControlAccessorDirective } from './form-control-accessor.directive';

describe('FormControlAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
